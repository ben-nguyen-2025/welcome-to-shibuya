download by running git clone
make sure you have node, npm installed


cd to welcome\ to\ shibuya
type: npm install to get all required dependencies
then start up the backend/frontend:
backend: php artisan serve
frontend: npm run dev
